# credit-core — Backlog (designed-but-not-built + new ideas)

> Snapshot 2026-06-29. Captures features requested but not yet implemented, so work can resume
> after context compaction. Each item still needs the brainstorming → spec → plan flow before build.

## Program state (what IS built + pushed)

| Branch | Built | Verified |
|---|---|---|
| `feat/mko-workbook-sp1-spec` | SP-1 data model (13 models, 4 enums) + all analysis/specs/plans | 14 backend tests |
| `feat/sp6-document-generation` | SP-6 doc generation: contract/petition/creditApplication PDFs + watermark + `GET /cases/:id/documents` + CaseView "Hujjatlar" panel + DEMO seed | real PDF (200, %PDF) |
| `feat/ux-messaging-improvements` | UX-B (copy login+pwd, phone) · C1 (admin SUBMIT) · C2 (action-panel) · D (collateral attach redesign) · A (Telegram inbox: DM + Saved, `Message.caseId` nullable, conversations/dm/saved/save-to-saved) | 21 tests + runtime smoke |

Reference docs: `docs/analysis/2026-06-29-mko-workbook-analysis.md`, the SP-1/SP-6/UX specs + plans under `docs/superpowers/`.

## NEW — Map feature (region picker + admin map + operation audit) [user req 2026-06-29]
- **Uzbekistan map**, admin-only view, regions (viloyatlar) ideally separated/distinct (GeoJSON of UZ regions; e.g. react-simple-maps / leaflet + UZ admin-1 GeoJSON).
- **Branch create form**: region picker is **OPTIONAL**. Pick a region from the map (e.g. choose "Buxoro" → map centers/zooms on Buxoro), pan around, OR enter **lat/lon directly**. Store coords on Branch (needs `Branch.lat`/`Branch.lon` + maybe `regionGeo`).
- **Operation audit on the map / per operation**: each operation shows **which API endpoint** ran and **at what time** (ties to the Activity-log item below — surface API+timestamp under each action).
- **Ideal UI** (ui-ux-pro-max), **dark theme** + **language (i18n)** aware (map labels/controls follow theme + locale).
- Decisions to make: map lib (leaflet vs react-simple-maps), GeoJSON source for UZ regions, whether coords are required, how region↔branch relate.

## Other backlog (from earlier 2026-06-29 requests)
1. **KATM reports** — ideal logic + UI (replace the manual `KatmInputs` placeholder in CaseView; ui-ux-pro-max).
2. **Assign people from an application** — designate users / guarantors / a separate person on a case.
3. **SLA timer polish** — make `DeadlineBadge` ("1 kun 23 soat qoldi") ideal.
4. **Duplicate-login inline check** — on user create, if login exists say "bu login bor" inline (backend already 409s on unique).
5. **Case visibility / sharing** — when submitting, option to make the case visible to other operators; settable by operator/moderator/admin/director.
6. **Activity log** — every action (file upload, write, add, transition) written to a DB log + shown in UI (extends `WorkflowEvent`; the map "which API when" overlaps this).
7. **Pause/resume** — ideal UI + correct logic (review current pause flow).
8. **DM/Saved file attachments** — make `Document.caseId` nullable so DM + Saved threads can carry image/file (currently text-only; see messages.module `createMessage` NOTE).
9. **SP-6 remaining documents** — договор рус, Протокол, РКЛ Ген, Акт согласования, Приказ, обложка, перечень + schedule/scoring docs (need SP-4 schedule + SP-5 scoring engines).
10. **Open PRs** — `gh` is not installed; PRs for the 3 branches must be opened via the compare URLs (or install `gh`).

## Recommended next order
Map feature is sizeable (new dep + GeoJSON + form integration + audit surface). Likely split:
(a) Branch coords + region picker (map in the branch form), (b) admin map view, (c) operation/API audit log (shared with item 6). Brainstorm (a) first.
